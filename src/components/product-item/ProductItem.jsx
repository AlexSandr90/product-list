import React from 'react';
import classes from './product-item.module.scss';

const ProductItem = () => {
    return (
        <section className={classes.product_item}>
            Product Item
        </section>
    );
};

export default ProductItem;